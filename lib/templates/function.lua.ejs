--[[
Function summary and description.

## Examples

    TODO

@version 0.0.1
@author NAME
]]--
return function(state, <%= typeof args !== 'undefined' && args.length ? args.join(', ') : '...' %>)
  -- Define function here
  return state
end

--- Summary of function.
-- Some description, can be over several lines.
<% if (typeof args !== 'undefined' && args.length) { args.forEach(arg => { -%>
-- @param <%= arg %> [description]
<% }) } else { -%>
-- @param ... [description]
<% } -%>
-- @return [description]
function main(ctx, <%= typeof args !== 'undefined' && args.length ? args.join(', ') : '...' %>)
  -- Define function here
  return ctx
end
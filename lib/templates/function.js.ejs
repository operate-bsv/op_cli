/**
 * @function    <%= name %>
 * @description [description]
<% if (typeof args !== 'undefined' && args.length) { args.forEach(arg => { -%>
 * @param       <%= arg %> [description]
<% }) } else { -%>
 * @param       args [description]
<% } -%>
 * @return      [description]
 */
function({ ctx, env }, <%= typeof args !== 'undefined' && args.length ? args.join(', ') : '...args' %>) {
  // Add function code
  return ctx;
}